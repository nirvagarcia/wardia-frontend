/**
 * Profile & Settings Page
 * Next.js App Router page for user profile and settings.
 */

import React from "react";
import { ProfileView } from "@/components/views/profile-view";

export default function Page(): React.JSX.Element {
  return <ProfileView />;
}
